name: CI Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    # Database service for integration testing
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: tododb
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Setup Node.js 18
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      # Step 3: Install dependencies
      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      # Initialize database schema
      - name: Initialize database
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d tododb -f ./database/database.sql

      # Step 4: Run backend tests
      - name: Run backend tests
        working-directory: ./backend
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: tododb
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: npm test

      # # Step 5: Build Docker images
      # - name: Build frontend Docker image
      #   run: |
      #     docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-frontend:latest ./frontend
      #     docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-frontend:${{ github.sha }} ./frontend

      # - name: Build backend Docker image
      #   run: |
      #     docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-backend:latest ./backend
      #     docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-backend:${{ github.sha }} ./backend

      # # Step 6: Login to Docker Hub
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_HUB_USERNAME }}
      #     password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      # # Step 7: Push Docker images to Docker Hub
      # - name: Push frontend images to Docker Hub
      #   run: |
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-frontend:latest
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-frontend:${{ github.sha }}

      # - name: Push backend images to Docker Hub
      #   run: |
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-backend:latest
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/devops-group1-backend:${{ github.sha }}
